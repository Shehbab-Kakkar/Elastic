---
actions:
  1:
    action: delete_indices
    description: >-
      Delete indices older by prefix 
    options:
      ignore_empty_list: True
      timeout_override:
      continue_if_exception: False
      disable_action: False
    filters:
    - filtertype: pattern
      kind: prefix
      value: mysql
